section#articles.articles
	tabset.tabs.tabs--light
		tab(ng-repeat="tab in tabs.single", heading="{{tab.title}}", active="tab.active")
			div(ng-repeat="feed in tab.content")
				a(href="#articles") {{feed.title}}
